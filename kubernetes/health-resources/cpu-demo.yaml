apiVersion: v1
kind: Pod
metadata:
  name: cpu-demo
spec:
  containers:
    - name: cpu-demo
      image: busybox:1.36
      command:
        - sh
        - -c
        - "while true; do :; done"
      resources:
        requests:
          cpu: "10m"
        limits:
          cpu: "50m"